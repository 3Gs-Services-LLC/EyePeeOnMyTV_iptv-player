<Window x:Class="EyePeeOnMyTV.Dialogs.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input"
        Width="480" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource AppBackgroundBrush}"
        Foreground="{StaticResource AppForegroundBrush}"
        KeyDown="Window_KeyDown">
    <StackPanel Margin="20">
        <TextBlock x:Name="PromptText" TextWrapping="Wrap" Margin="0,0,0,10" />
        <TextBox x:Name="ValueBox" Padding="8,6" Background="{StaticResource AppSurfaceBrush}"
                 Foreground="{StaticResource AppForegroundBrush}" BorderThickness="0"
                 CaretBrush="White" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" Width="80" Margin="0,0,8,0" Click="CancelButton_Click"
                    Background="{StaticResource AppSurfaceBrush}" Foreground="{StaticResource AppForegroundBrush}"
                    BorderThickness="0" Padding="8,6" />
            <Button Content="OK" Width="80" Click="OkButton_Click" IsDefault="True"
                    Background="{StaticResource AppAccentBrush}" Foreground="White"
                    BorderThickness="0" Padding="8,6" />
        </StackPanel>
    </StackPanel>
</Window>
